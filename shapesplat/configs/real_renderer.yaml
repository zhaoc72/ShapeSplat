seed: 7
device: auto

image:
  size: 64
  input_path: null

renderer:
  # 这里还没有真实 CUDA 3DGS renderer。auto 会优先尝试 real，失败后 fallback soft。
  backend: auto
  # 后续用户可以填写真实 renderer 的 Python module path 和 class 名称。
  real_renderer_module: null
  real_renderer_class: null
  fallback_to_soft: true
  beta_depth: 1.5
  min_sigma_px: 1.0
  max_sigma_px: 4.0
  contribution_mode: visibility_weighted
  ownership_eps: 1.0e-6
  render_object_contributions: true
  render_depth: true
